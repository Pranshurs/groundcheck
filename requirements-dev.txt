# Dev / CI dependencies — deliberately light.
# The package's torch/transformers imports are lazy, so tests, the API, and the benchmark
# all run on the heuristic + mock backends WITHOUT installing torch. This keeps CI fast.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-dotenv>=1.0.0
openai>=1.30.0
pytest>=8.0.0
httpx>=0.27.0
