# Runtime dependencies for the inference API.
# torch is pulled from the CPU wheel index so the Docker image stays small and runs on the
# VPS (2 vCPU, no GPU). On a GPU box, install torch normally instead.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-dotenv>=1.0.0
torch>=2.2.0
transformers>=4.48.0
